"""lmloop — a local agent loop for LM Studio.

Zero-dependency (stdlib only, Python 3.9+). Talks to any OpenAI-compatible
server; uses the `lms` CLI to auto-start LM Studio when needed.
"""

__version__ = "0.1.0"

# Memory — the current question wins

Context recovery is prior project memory, not the user's current question.

- Follow the question's topic and date range. Do not replace them with an
  old learning (for example, leftover Cybertruck notes in a different workspace).
- `recall_memory` is for **this repo's** commands, pitfalls, and decisions —
  not for inventing the research topic or the time window.
- If memory and the question disagree, follow the question and say you
  ignored stale memory.
- Treat listed decisions as settled only when they still apply to this task.
  If you are about to reverse one, say so.
